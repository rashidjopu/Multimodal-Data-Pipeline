##############################################################################
# Multimodal E-Commerce Data & AI Pipeline - requirements.txt
# Python 3.10 or 3.11 recommended (Airflow 2.9+ does not yet support 3.12)
##############################################################################

# ---- Core data handling -----------------------------------------------
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0            # fast parquet/CSV I/O for the data lake layer
faker==25.8.0              # realistic mock review/product data generation

# ---- Schema validation & config ----------------------------------------
pydantic==2.7.4
pydantic-settings==2.3.4
python-dotenv==1.0.1

# ---- LLM / AI enrichment clients ---------------------------------------
openai==1.35.3             # OpenAI SDK (GPT-4o/GPT-4o-mini structured outputs)
anthropic==0.28.1          # Claude SDK (alternative structured-output provider)
tenacity==8.4.1            # retry/backoff for flaky LLM & video API calls

# ---- HTTP / API clients (Higgsfield video-gen + generic REST) ----------
requests==2.32.3
httpx==0.27.0

# ---- Data warehouse layer ------------------------------------------------
sqlalchemy==2.0.31
psycopg2-binary==2.9.9     # Postgres driver (SQLite needs no driver, built into Python)
alembic==1.13.2            # schema migrations for the warehouse

# ---- Orchestration -------------------------------------------------------
apache-airflow==2.9.2
# Optional alternative orchestrator (pick ONE of airflow / mage-ai in practice)
# mage-ai==0.9.72

# ---- Data quality ----------------------------------------------------------
great_expectations==0.18.19

# ---- Dashboard / observability ------------------------------------------
streamlit==1.36.0
plotly==5.22.0

# ---- Logging ----------------------------------------------------------
loguru==0.7.2

# ---- Testing -------------------------------------------------------------
pytest==8.2.2
pytest-mock==3.14.0
responses==0.25.3          # HTTP mocking for Higgsfield / LLM API tests

##############################################################################
# Notes:
# - Airflow has notoriously strict dependency constraints. In production,
#   install it using the official constraints file, e.g.:
#     pip install "apache-airflow==2.9.2" \
#       --constraint "https://raw.githubusercontent.com/apache/airflow/constraints-2.9.2/constraints-3.11.txt"
# - great_expectations pulls in its own pandas/sqlalchemy pins; if you hit
#   resolver conflicts, install Airflow and Great Expectations in separate
#   virtual environments (see README.md "Environment Isolation" section).
##############################################################################
